{
  "name": "esco-api",
  "module": "src/handler.ts",
  "type": "module",
  "scripts": {
    "build": "bun build src/handler.ts --outdir dist",
    "dev": "bun --watch run --port 3560 src/handler.ts",
    "clean": "rimraf dist",
    "test": "bun test",
    "test:dev": "bun --watch test"
  },
  "devDependencies": {
    "bun-types": "latest",
    "rimraf": "^5.0.5"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "valibot": "^0.20.1"
  }
}